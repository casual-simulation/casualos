import { ModerationController } from './ModerationController';

describe('ModerationController', () => {
    let controller: ModerationController;

    beforeEach(() => {
        controller = new ModerationController();
    });

    describe('reportInst()', () => {
        it('should record the report in the store', () => {});
    });
});
