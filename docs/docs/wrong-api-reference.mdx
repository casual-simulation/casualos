---
id: wrong-api-reference
title: API Reference
sidebar_label: API Reference
description: Actions affect the data in bots and also do things to the outside world.
---

import { 
    objectTableOfContents,
    ObjectMembers,
    classTableOfContents,
    setLinkMap,
    DebugObject,
    getReturnType,
    getProperty,
    RenderTree,
    getTypeReferences
} from '@site/util/doc.jsx'
// import './typeLinkMap';
import DefaultLibrary from '@api/DefaultLibrary';
import createDefaultLibrary, { references, requiredReferences } from '@api/createDefaultLibrary';
// import AddDropGridTargetsAction from '@api/AddDropGridTargetsAction'
import Heading from '@theme/Heading';

export const os = getProperty(getProperty(getReturnType(createDefaultLibrary), 'api'), 'os');

export const toc = [
    { value: 'Methods', id: 'methods', level: 1 }, 
    { value: 'OS Actions', id: 'os-actions', level: 2 }, 
    // { value: 'Vector2', id: 'vector2', level: 2 }, 
    ...objectTableOfContents(os), 
    // { value: 'Vector3', id: 'vector3', level: 2 },
    // ...classTableOfContents(Vector3),
    { value: 'Types', id: 'types', level: 1 }, 
];

<Heading as='h1' id='methods'>Methods</Heading>

<Heading as='h2' id='os-actions'>OS Actions</Heading>

<DebugObject object={requiredReferences} />

<Heading as='h1' id='Types'>Types</Heading>
