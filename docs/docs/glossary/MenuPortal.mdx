import { Title, Table } from "../components/glossaryItems.jsx";

<Title title="What is it?" containerStyle={{color:"white"}} style={{color:"#c4c40c"}} />

The menuPortal is a popup of [bot](#bot) generated and handled inputs.

<Title title="Why does it exist?" containerStyle={{color:"white"}} style={{color:"#c4c40c"}} />

The menuPortal is designed to provide a means of interaction provided, and handled by a [bot](#bot) or if desired; multiple bots.
