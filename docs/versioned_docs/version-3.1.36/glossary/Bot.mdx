import { GlossaryRef } from './index';

Bots are containers of data and behavior. Bots live in <GlossaryRef term='instance'>instances</GlossaryRef> and each bot has a unique identifier.
Additionally, a bot contains one or more <GlossaryRef term='tag'>tags</GlossaryRef> that define the properties and behavior of the bot.